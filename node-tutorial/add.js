const add = (a, b) => {
	return a + b;
};

const subtract = (a, b) => {
	return a - b;
};

// module.exports.divide = (a, b) => {
// 	return a / b;
// };

exports.divide = (a, b) => {
	return a / b;
};

// module.exports = subtract = (a, b) => {
// 	return a - b;
// };

// console.log(add(3, 4));

// module.exports = add;

// module.exports = { add, subtract };
