// const newAdd = (a, b) => {
// 	return a + b;
// };

// const newSubtract = (a, b) => {
// 	return a - b;
// };

export const newAdd = (a, b) => {
	return a + b;
};

export const newSubtract = (a, b) => {
	return a - b;
};

// export default (a, b) => {
// 	return a + b;
// };

// export default newAdd;
// export default {
// 	newAdd,
// 	newSubtract,
// };
